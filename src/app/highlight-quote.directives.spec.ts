import { HighlightQuoteDirectives } from './highlight-quote.directives';

describe('HighlightQuoteDirectives', () => {
  it('should create an instance', () => {
    const directive = new HighlightQuoteDirectives();
    expect(directive).toBeTruthy();
  });
});

